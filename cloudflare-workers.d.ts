declare module "cloudflare:workers" {
  // The starter's optional D1 binding is not used by this static application.
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  export const env: Record<string, any>;
}
